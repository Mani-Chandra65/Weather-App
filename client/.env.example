# Example environment variables for client (create as .env in client/)
# For Netlify, add this as an environment variable in Netlify dashboard

# Backend API URL - Replace with your Render service URL
VITE_API_URL=https://your-app-name.onrender.com/api
