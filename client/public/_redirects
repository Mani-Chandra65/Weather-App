# Netlify Redirects for SkyScope Pro

# Proxy API calls to Render backend (Update this URL after deploying to Render)
/api/*  https://skyscope-pro-backend.onrender.com/api/:splat  200

# Handle client-side routing (React Router) - must be last
/*    /index.html   200
