# Example environment variables for server (create as .env in server/)
# For Render, add these as environment variables in Render dashboard

# OpenWeatherMap API Key
WEATHER_API_KEY=your_openweather_api_key_here

# Port (Render will set this automatically)
PORT=10000

# Environment
NODE_ENV=production
